{
  "name": "@domqa/extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json && cp manifest.json popup.html dist/ && cp -r dist/src/* dist/ 2>/dev/null || true",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@domqa/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.322",
    "typescript": "^5.7.3"
  }
}
