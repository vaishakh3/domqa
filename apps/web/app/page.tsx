import Link from "next/link";
import { Card, Container, PageShell } from "../components/ui";

const features = ["Element-level issue capture on live sites", "DOM fingerprinting with fallback locator strategies", "Automatic screenshot, console, and network context", "Developer-friendly dashboard for triage and collaboration"];

export default function LandingPage() {
  return <PageShell><Container className="py-20"><header className="grid gap-10 lg:grid-cols-2 lg:items-center"><div><p className="mb-4 inline-flex rounded-full border border-violet-500/40 px-3 py-1 text-sm text-violet-200">Browser extension + dashboard</p><h1 className="text-5xl font-semibold tracking-tight">Report frontend bugs with exact element context.</h1><p className="mt-6 max-w-2xl text-lg text-slate-300">Capture issues directly on live websites, attach them to the real UI element, and send developers the technical context they need to fix faster.</p><div className="mt-8 flex gap-4"><Link className="rounded-lg bg-violet-500 px-5 py-3 font-medium text-white" href="/signup">Start free</Link><Link className="rounded-lg border border-slate-700 px-5 py-3 font-medium text-slate-200" href="/login">Sign in</Link></div></div><Card><div className="space-y-5"><h2 className="text-xl font-semibold">Why it beats screenshots</h2><ul className="space-y-3 text-slate-300">{features.map((item) => <li key={item}>• {item}</li>)}</ul></div></Card></header><section className="mt-20 grid gap-6 md:grid-cols-3">{[{title:"1. Inspect",body:"Toggle feedback mode in the extension and click the exact element in the live DOM."},{title:"2. Capture",body:"DOMQA stores selectors, fingerprints, console errors, failed requests, and a viewport screenshot."},{title:"3. Collaborate",body:"Developers triage in a central dashboard with comments, filters, statuses, and relocation hints."}].map((item)=><Card key={item.title}><h3 className="text-lg font-semibold">{item.title}</h3><p className="mt-3 text-slate-300">{item.body}</p></Card>)}</section></Container></PageShell>;
}
