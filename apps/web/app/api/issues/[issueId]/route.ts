import { NextResponse } from 'next/server';
import { requireApiUser } from '@/server/auth/guards';
import { getIssue, updateIssue } from '@/server/services/issues';
export async function GET(_: Request, { params }: { params: Promise<{ issueId: string }>}) { try { const user = await requireApiUser(); const { issueId } = await params; const issue = await getIssue(user.id, issueId); if (!issue) return NextResponse.json({ error: 'Not found' }, { status: 404 }); return NextResponse.json(issue); } catch (error) { if (error instanceof Response) return error; return NextResponse.json({ error: 'Unauthorized' }, { status: 401 }); } }
export async function PATCH(request: Request, { params }: { params: Promise<{ issueId: string }>}) { try { const user = await requireApiUser(); const { issueId } = await params; return NextResponse.json(await updateIssue(user.id, issueId, await request.json())); } catch (error) { if (error instanceof Response) return error; return NextResponse.json({ error: error instanceof Error ? error.message : 'Unable to update issue' }, { status: 400 }); } }
export async function POST(request: Request, context: { params: Promise<{ issueId: string }>}) { const form = await request.formData(); if (form.get('_method') === 'PATCH') { return PATCH(new Request(request.url, { method: 'PATCH', headers: request.headers, body: JSON.stringify(Object.fromEntries(form.entries())) }), context); } return NextResponse.json({ error: 'Method not allowed' }, { status: 405 }); }
